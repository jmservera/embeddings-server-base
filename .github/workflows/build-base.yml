name: Build Base Image

on:
  push:
    branches: [main]
    paths:
      - 'Dockerfile'
  workflow_dispatch:
    inputs:
      model:
        description: 'Model to cache'
        default: 'intfloat/multilingual-e5-base'
        type: string

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - variant: cpu
            tag_suffix: ""
            install_openvino: "false"
          - variant: openvino
            tag_suffix: "-openvino"
            install_openvino: "true"
    steps:
      - uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push ${{ matrix.variant }} variant
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          build-args: |
            INSTALL_OPENVINO=${{ matrix.install_openvino }}
          tags: |
            ghcr.io/${{ github.repository }}:3.12-slim-multilingual-e5-base${{ matrix.tag_suffix }}
            ${{ matrix.variant == 'cpu' && format('ghcr.io/{0}:latest', github.repository) || '' }}
          secrets: |
            HF_TOKEN=${{ secrets.HF_TOKEN }}
